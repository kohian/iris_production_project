# name: Build and Push Docker Image

# on: 
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Authenticate to Google Cloud
#         uses: google-github-actions/auth@v2
#         with:
#           credentials_json: '${{ secrets.GCP_SA_KEY }}'

#       - name: Configure Docker
#         run: gcloud auth configure-docker us-central1-docker.pkg.dev
        
#       - name: Build Docker image
#         run: |
#           docker build -t asia-southeast1-docker.pkg.dev/PROJECT_ID/REPO/myapp:latest .

# us-central1-docker.pkg.dev/iris-ml-production/ian-docker-artifacts

#       - name: Push Docker image
#         run: |
#           docker push asia-southeast1-docker.pkg.dev/PROJECT_ID/REPO/myapp:latest